# Required checkpoint

Place the trained checkpoint here with this exact filename:

`kla_renaf_sr_x2.pt`

The supplied source archive did not contain the checkpoint, so the final trained file must be added before submission.
